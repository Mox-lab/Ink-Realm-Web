================================================================
墨域 · 创作流程规范参考（业务对齐版 v2.1）
================================================================

> 本文档依据后端 `ConceptAgent` / `SettingAgent` / `OutlineAgent` / `CharacterExtractionAgent`
> / `LoreAgent` 与 `AiAgentController` 的实际实现，以及前端 `Outline.jsx` / `Character.jsx`
> / `Lore.jsx` / `utils/parse.js` 整理，是**构思→设定→大纲→人物→设定库**创作链路的**权威规格说明**。
> 原《三层自由文本指令集》已按实际业务重构为「两段式结构化」大纲流程；人物与设定则分别
> 走「设定集 markdown」与「结构化 CharacterProfile JSON」两条产物线。
> 真实的 LLM 提示词以代码 `*.java`（AiAgent）为准，本文档是它的可读规格化。

================================================================
第 0 节：一句话结论
================================================================

系统不走「顶层企划书 → 卷级 → 章级」三层自由文本，而是沿五阶段链路推进
（构思 → 设定 → 大纲 → 写作 → 润色），其中与「大纲生成规范」直接相关的是前三阶段：

1. 阶段1 构思：一句话灵感 → `题材蓝图`（核心冲突/卖点/读者/基调/篇幅），见第 8.1 节。
2. 阶段2 设定：题材蓝图 → `设定集`（世界观 + 主要人物档案），见第 8.2 节。
3. 阶段3 大纲（本文档主体）：
   3.1 输入：`theme`（题材蓝图/主题）+ 可选 `setting`（设定集，来自阶段2）。
   3.2 第一段 卷规划：LLM 生成可编辑的结构化卷骨架
       （`## 第 X 卷 · 卷名` + `卷主线:` + `章数:X`）。
   3.3 第二段 逐卷展开：每卷独立生成逐章细纲
       （`### 第 N 章 章名` + 主线/冲突/钩子/细纲）。
   3.4 自检：每段生成后经 `selfCheck`（主题一致/连贯/编号连续），
       未通过则带反馈重试一次。
4. 旁支 人物抽取：从自然语言文本抽取结构化 `人物卡`（CharacterProfile JSON），
   用于人物关系图与一致性约束，见第 8.4 节。
5. 旁支 设定问答：基于设定库 RAG 回答世界观/人物/剧情问题，维护一致性，见第 8.6 节。

> 原规范里的「三幕式 / 人物金字塔 / 爽点 / 节奏检查」等精华，已下沉为
> **卷主线与章纲的内容质量基线**（见第 5 节），而非独立的、系统需解析的产物
> ——因为大纲解析器只认 `## 第 X 卷` / `卷主线:` / `章数:` / `### 第 N 章`。
> 人物与世界观的「自洽」则由「设定集（第 8.2 节）随请求带入」+「selfCheck ① 设定一致」
> 共同保证，不以独立自由文本产物存在。

================================================================
第 1 节：流程总览与接口
================================================================

创作流程共 5 步：构思(concept) → 设定(setting) → 大纲(outline) →
章节(writing) → 润色(polish)。大纲是第 3 步。

| 步骤       | 接口                     | 作用                                 | 前端入口                         |
|------------|--------------------------|--------------------------------------|----------------------------------|
| 构思       | POST /api/concept        | 一句话灵感 → 题材蓝图                | Outline 页「生成题材蓝图」按钮(以主题作灵感回填主题框)|
| 设定       | POST /api/setting        | 题材蓝图 → 设定集(世界观+主要人物)   | Outline 页「AI 生成设定集」按钮(回填设定集框,作 outline 的 `setting`)|
| 卷规划     | POST /api/outline/plan   | 生成可编辑卷骨架                     | 「规划卷结构」                   |
| 应用骨架   | 前端 applyPlan           | 用(可能编辑过的)卷规划重建大纲骨架   | 「应用卷规划」                   |
| 逐卷展开   | POST /api/outline/volume | 展开指定卷的逐章细纲                 | 每卷「应用本卷」/「重新规划本卷」、或「展开全部卷细纲」 |
| 全量快捷   | POST /api/outline        | 规划 + 逐卷展开 + 自检一次性产出     | （保留入口）                     |
| 人物抽取   | POST /api/character      | 文本 → 结构化人物卡(CharacterProfile)| `Character.jsx`「抽取」          |
| 设定问答   | POST /api/lore           | RAG 基于设定库回答                   | `Lore.jsx`                      |

硬约束：
- 必须先有卷规划才能展开：`/outline/volume` 无 `volumePlan` 返回参数错误。
- 卷数/章数**由 LLM 决定**：建议 3-7 卷，每卷 **100-300 章量级**
  （可上下浮动，厚重卷可 300-400+，过渡卷可 <100）；全书总量 = 各卷
  `章数` 之和（数百到上千章）。前端不再传「目标章节数」。
- 每卷章号**全局连续**：由 `buildVolumes` 按「章数」顺序分配起止章号
  （卷1: 1~X，卷2: X+1~...）。
- 设定集（`setting`）作为约束随请求带入：大纲的 卷主线/章纲 必须与设定集的世界观、
  人物、势力一致；`selfCheck` ① 会核对「无凭空新设定/新势力/新人设」。

================================================================
第 2 节：第一段 —— 卷规划（Volume Plan）
================================================================

触发：`planVolumes(blueprint, setting)`（经 `/outline/plan`）。
产出写入 `volumePlan` 与正文骨架，可在前端 textarea 自由编辑后「应用卷规划」。

【2.1 严格输出格式】（解析器据此解析，务必遵守）

    # 卷规划(共 N 卷,约 M 章)

    ## 第 1 卷 · 卷名
    卷主线:用 3-5 句话详细描写本卷核心事件、主要转折点、人物成长与情绪曲线,要具体可落地
    章数:X

    ## 第 2 卷 · 卷名
    卷主线:...
    章数:Y

    （依卷数重复；卷标题用「·」分隔；`卷主线:` 紧随卷标题；`章数:` 为整数）

【2.2 格式硬约束】（来自 `AiAgentController.buildVolumes` 解析）
- 卷标题必须匹配 `## 第 X 卷 · 卷名`（分隔符 `·` / `:` / `：` 均可，规范统一用
  `·`，卷号用阿拉伯数字）。
- 必须紧跟 `卷主线:`（解析为卷的 `arc`，用于后续展开该卷细纲与展示）。
- 必须有 `章数:X`（整数）。三项缺一不可，否则该卷解析失败。
- 降级：若完全不符合格式，系统降级为单卷（卷名「正文」，
  `FALLBACK_CHAPTERS=20` 章）。应避免触发降级。

【2.3 内容质量基线】（来自 `OutlineAgent.planVolumes` 提示词）
1. 卷数依题材体量合理决定，建议 **3-7 卷**；
2. **每卷章数建议 100-300 章量级，可依剧情体量浮动（可更多或更少），
   不要为凑数硬性统一**；
3. 各卷「章数」之和即为全书总章数（通常数百到上千章），不必凑整数；
4. 卷名 **2-8 字**，有主题意境、不剧透；
5. 每卷「卷主线」必须详细具体，覆盖**核心冲突、关键转折、人物命运与重要伏笔**，
   确保后续能展开该卷上百章、不写空；
6. 卷主线要体现「起承转合」的递进，后卷必须建立在前卷结局之上；
7. 必须有明确的**全局主线**（如成长/复仇/探秘），各卷围绕它推进；
8. **末卷要收束主要伏笔，不烂尾**；
9. 全程中文，不要输出多余解释。

【2.4 前端可编辑性】
- 增删 `## 第 N 卷` 块可调整卷数；改卷名/章数/卷主线后「应用卷规划」即重建骨架。
- 「修改本卷」弹窗（`VolumeEditModal`）可逐章编辑章号/标题/细纲并增删，
  `serializeVolumes` 回写。

================================================================
第 3 节：第二段 —— 逐卷展开细纲（Per-Volume Chapter Outline）
================================================================

触发：`expandVolume(...)`（经 `/outline/volume`，可循环调用实现「展开全部」）。
携带【整书卷规划】+【前情：已完成各卷摘要】保证连贯；生成后 `selfCheck` 自检。

【3.1 严格输出格式】（解析器据此解析）

    ### 第 {{startChapter}} 章 章名
    - 主线:本章一句话核心事件
    - 冲突:本章关键矛盾
    - 钩子:章尾悬念
    - 细纲:(2-4 条要点,描述场景/转折/人物反应,承接上一章、铺垫下一章)

    ...依次到 ### 第 {{endChapter}} 章

（章号从本卷 `startChapter` 到 `endChapter`，全局连续；
`startChapter/endChapter` 由第一段「章数」推导）

【3.2 格式硬约束】（来自 `OutlineAgent.expandVolume` 提示词 + `parse.js`）
- 章号连续递增，从 `startChapter` 到 `endChapter`，**绝不跳号、绝不重复**；
- 每章细纲必须紧跟前一章结局，场景/人物/伏笔自然衔接，严禁突兀转场；
- **本卷首章要承接【前情】中上一卷的结局**；**本卷末章要为下一卷埋下过渡钩子**；
- 章名 **4-8 字**，简洁有画面感；细纲具体可执行，不是空话；
- 全程紧扣【题材蓝图】主题，不偏离、不引入与前文矛盾的设定；
- 全局伏笔要在对应卷回收，新增伏笔要登记以便后续回收；
- 全程中文，不要输出多余解释；**只输出本卷章节**，不要重复卷规划。

【3.3 解析与回写】（前端 `utils/parse.js`）
- `parseOutlineVolumes`：把 `### 第 N 章 章名` 解析为章节点，其下
  `- 主线/冲突/钩子/细纲` 作为该章 `summary`（在流程图与详情中展示）。
- `insertVolumeChapters`：将单卷章纲 markdown 插入到该卷 `## 第 X 卷` 标题之后、
  下一卷标题之前（就地回写，可中断、可重试）。
- `serializeVolumes`：把「卷→章」结构反序列化为文本（卷名非空 →
  `## 第 X 卷 · 卷名` + `卷主线:`；每章 → `第 N 章 标题` + summary），
  供内联编辑回写，与解析互逆。

================================================================
第 4 节：自检机制（selfCheck）
================================================================

- 每项展开后，`selfCheck(theme, segment, context)` 核对三点：
  ① 主题一致（无凭空新设定/新势力/新人设）；
  ② 连贯性（章章衔接、承接前卷结局、无重复已发生事件）；
  ③ 编号连续（章号连续、无跳号重复）。
- 输出 `结论:通过` 或 `结论:需修正` + 问题清单（最多 6 条）。
- 未通过则带反馈重试一次（把问题清单追加到 blueprint 重新生成该卷）；
  仍失败则返回错误提示（可能因思维链烧光 token 导致空输出）。

================================================================
第 5 节：原三层精华 → 融入现有格式（质量基线，不单独解析）
================================================================

原《三层指令集》的结构化思维仍有效，只是不再作为独立产物，而是内化为要求
（人物与世界观的「自洽」另由第 8 节「设定集 + 人物卡」线保障）：

- 人物一致性：在卷主线与章纲中保持人设/欲望/恐惧/关系一致；建议把人物档案
  写进 `setting`（设定集），随请求带入，约束全卷人设不漂移。
- 伏笔管理：卷主线埋设伏笔并暗示「计划揭晓卷」；章纲 `钩子` 与 `细纲` 中
  登记/回收伏笔。解析器只认文本，需 LLM 自觉维护伏笔账本，避免挖坑不填。
- 三幕/节奏：卷规划天然对应三幕——开局卷（立人设+亮金手指）、
  中盘卷（地图升级+势力博弈+伏笔）、终局卷（伏笔回收+终极战）。
  卷序安排应体现此弧光。
- 爽点/情绪曲线：章纲 `细纲` 中应包含爽点类型（打脸/收宝/升级/悬念/暧昧）
  与情绪目标；**前 3 章每章至少 1 个不重复的爽点**，每 10 章一次小高潮。
- 章尾钩子轮换：`钩子` 类型可轮换（悬念/危机/反转/情感/信息），避免套路重复。

================================================================
第 6 节：与原规范差异对照
================================================================

| 维度           | 原规范（三层自由文本）                  | 实际业务（两段式结构化）                          |
|----------------|-----------------------------------------|--------------------------------------------------|
| 产物层级       | 顶层企划书 + 卷级 + 章级（三层）        | 卷规划 + 逐卷章纲（两段）                        |
| 顶层企划书     | 独立输出（三幕/人物金字塔/伏笔总览）    | 作为输入 `blueprint`+`setting`，不单独产出/解析  |
| 卷结构格式     | 自由表格                                | 强制 `## 第 X 卷 · 卷名`+`卷主线:`+`章数:X`     |
| 章纲字段       | 核心事件+爽点+钩子+伏笔（30-50字）      | `主线/冲突/钩子/细纲` 四要素（markdown）         |
| 卷数章数       | 推荐 5-6 卷、每卷 200-300 章            | 3-7 卷、每卷 100-300 章量级（LLM 定，可浮动）    |
| 连贯保证       | 人工                                    | `prevVolumes` 前情 + `selfCheck` 自检重试        |
| 可编辑/可中断  | 无                                      | 卷规划可编辑、逐卷展开可中断可重跑                |
| 设定/世界观    | 顶层企划书内自由描述                     | `设定集`(第 8.2 节)作 `setting` 带入,约束全卷    |
| 人物           | 人物金字塔自由描述                       | `设定集·主要人物` + `人物卡`抽取(第 8.4 节)JSON  |

================================================================
第 7 节：交付核对清单（适配实际）
================================================================

【卷规划】
- [ ] 卷标题格式 `## 第 X 卷 · 卷名`、含 `卷主线:`、含 `章数:X`
      （三项齐全，不触发降级）
- [ ] 卷数 3-7；每卷章数 100-300 量级（可浮动）；卷名 2-8 字
- [ ] 卷主线覆盖 核心冲突/转折/人物命运/伏笔，起承转合递进
- [ ] 全局主线明确；末卷收束主要伏笔

【逐卷章纲】
- [ ] 章号从 `startChapter` 连续到 `endChapter`，无跳号/重复
- [ ] 首章承接前卷结局；末章埋下卷间钩子
- [ ] 每章含 `主线/冲突/钩子/细纲` 四要素；章名 4-8 字
- [ ] 伏笔有登记/回收；前 3 章爽点不重复；紧扣主题、中文

【设定集 / 人物（上游，第 8 节）】
- [ ] 设定集含 `## 世界观`(时空背景/力量体系/主要势力/核心规则) 与 `## 主要人物`
- [ ] 主要人物至少含 主角/主要对手/盟友/长者导师；性格动机能驱动冲突、秘密可成钩子
- [ ] 人物卡(CharacterProfile)字段齐全；关系用 目标名+关系类型 表达，供关系图连线
- [ ] 设定集作为 `setting` 随大纲请求带入；selfCheck ① 核对世界/人物无凭空新增

================================================================
第 8 节：设定集与人物（阶段 1-2 与人物抽取 / 设定问答）
================================================================

前 7 节聚焦「大纲（阶段3）」。本节补齐上游的【概念】【设定】与旁支的【人物抽取】
【设定问答】，它们共同决定大纲的世界观自洽与人物一致性。

----------------------------------------------------------------
8.1 阶段1 构思 —— 题材蓝图（ConceptAgent → /api/concept）
----------------------------------------------------------------
输入：`inspiration`（一句话灵感）+ 可选 `genre`（类型：玄幻/都市/科幻/历史/言情…）。
输出 markdown（≤600 字），严格结构：

    # 题材蓝图
    ## 一句话简介        （主角是谁、要做什么、最大障碍）
    ## 核心冲突          （2-3 条：人vs人 / 人vs环境 / 人vs自我）
    ## 卖点与爽点        （主卖点 + 1-2 次卖点）
    ## 目标读者          （画像与偏好）
    ## 整体基调          （热血/暗黑/治愈/悬疑/轻松/沉重 选 1-2）
    ## 预估篇幅          （短/中/长篇 + 章节区间）

硬约束：
- 严禁编造作者未提供的人名，用「主角/对手/导师」等代号；
- 语言精炼不废话；它是后续 `设定集` 与 `大纲` 的 `blueprint` 输入。

----------------------------------------------------------------
8.2 阶段2 设定 —— 设定集（SettingAgent → /api/setting）
----------------------------------------------------------------
输入：`blueprint`（阶段1 题材蓝图）+ 可选 `tone`（基调关键词，约束氛围）。
输出 markdown（≤1500 字），严格结构（解析器虽不强制解析，但作为 `setting`
整体带入大纲，需结构清晰以便 LLM 检索与 selfCheck 比对）：

    # 设定集

    ## 世界观
    ### 时空背景          （朝代/年代/地理格局，2-3 句）
    ### 力量体系          （武学/魔法/科技/血脉分级；无则写"无超自然体系"）
    ### 主要势力          （3-5 方：`- 势力名 — 一句话定位 — 立场(中立/敌对/盟友)`）
    ### 核心规则          （世界运行 2-3 条铁律，如"先天之上可御气飞行"）

    ## 主要人物
    ### {角色名}
    - 身份:
    - 年龄:
    - 性格:(3-4 个形容词)
    - 外貌:(一句话)
    - 动机:他/她最想要什么
    - 秘密:不为人知的关键信息
    - 关系:与其他角色的关联

    至少包含:主角、主要对手、一位盟友、一位长者/导师。

内容质量基线：
1. 姓名贴合世界观（古代背景避免现代名）；
2. 性格与动机要能驱动冲突，不写「老好人」型扁平角色；
3. 每个秘密都要能成为后续剧情钩子；
4. 整体 ≤1500 字，全程中文。

> 该设定集即大纲请求里的 `setting` 参数：写入 `/outline/plan` 与
> `/outline/volume` 后，大纲的 卷主线/章纲 必须与之吻合（见第 1 节硬约束、
> 第 4 节 selfCheck ①）。

----------------------------------------------------------------
8.3 设定集 → 大纲 的衔接（一致性来源）
----------------------------------------------------------------
- 设定集作为 `setting` 字符串，随 `OutlineRequest.setting` 进入 `planVolumes` 与
  `expandVolume`；提示词要求「不偏离、不引入与前文矛盾的设定」。
- `selfCheck(theme, segment, context)` 的 ①主题一致 即核对：
  **无凭空新设定 / 新势力 / 新人设**。若偏离，带反馈重试一次。
- 因此「人物不漂移」的首要手段，是把人物档案写进 `设定集·主要人物` 随请求带入，
  而非依赖独立自由文本。

----------------------------------------------------------------
8.4 旁支 人物抽取 —— 结构化人物卡（CharacterExtractionAgent → /api/character）
----------------------------------------------------------------
与「设定集里的人物段落」不同，这里是**从任意自然语言文本**抽取出
**结构化 JSON（CharacterProfile）**，用于前端人物关系图与持久化，而非 markdown。

字段表（与设定集人物段落的对应）：

| 字段           | 含义            | 对应设定集人物项 | 约束                              |
|----------------|-----------------|------------------|-----------------------------------|
| name           | 姓名            | {角色名}         | 必填；缺失则无法建卡              |
| age            | 年龄            | 年龄:            | 整数；无法解析为 null             |
| gender         | 性别            | （未单列）       | 男/女/其他 三选一；否则 null      |
| identity       | 身份/职业       | 身份:            | 自由文本                          |
| personality    | 性格特点        | 性格:            | 自由文本                          |
| appearance     | 外貌描述        | 外貌:            | 自由文本                          |
| weapon         | 武器/法宝/技能  | （未单列）       | 自由文本                          |
| background     | 背景故事        | （未单列）       | 自由文本                          |
| relationships  | 关系列表        | 关系:            | 数组,每条 {target,type,note};无则 [] |

硬约束（来自抽取提示词）：
- 字段缺失填 null，**严禁编造**；
- `relationships` 用 `target`(对方姓名) + `type`(师徒/兄弟/宿敌/恋人/父子…) + 可选 `note`；
- **只输出 JSON，不附加任何说明文字**（前端 `Character.jsx` 直接解析为卡片与关系图）。

前端消费：`Character.jsx` 用人物卡渲染卡片与关系图（`parseEdges` 据
`relationships.target` 连线）；`CharacterAddModal` 手动补卡时关系以名字串存储，
同样供图谱连线。抽取卡与设定集人物应保持一致（同源则无漂移）。

----------------------------------------------------------------
8.5 人物一致性约束建议（给提示词/流程）
----------------------------------------------------------------
- 规划卷结构、展开章纲时，将「设定集·主要人物」视作人设基线；
- 章纲 `细纲` 中涉及人物的行动/动机/关系，须与人物卡 `动机/秘密/关系` 自洽；
- 若需新增人物，应在设定集登记其 身份/动机/秘密，再进入大纲，避免 selfCheck ① 触发重试。

----------------------------------------------------------------
8.6 旁支 设定问答 —— 设定库 RAG（LoreAgent → /api/lore 等）
----------------------------------------------------------------
用于大纲/写作期的一致性维护，而非生成产物：

- `POST /api/lore`：基于检索到的设定片段回答作者问题；有答案则注明出处，
  无答案则直说「设定库中未找到」，矛盾设定给出 2 个调和方案。
- `POST /api/lore/import`：把设定文本（如设定集全文）导入知识库（`text=设定集`），
  供后续 RAG 检索。
- `POST /api/lore/search`：检索调试，查看设定库命中片段。

> 推荐流程：阶段2 产出设定集后，调 `/api/lore/import` 把其作为设定库种子，
> 后续大纲/写作阶段用 `/api/lore` 做一致性问答。

================================================================
指令结束
================================================================
